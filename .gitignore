# Dependencies
node_modules/
package-lock.json
# (Keep package-lock.json if you want lockfiles, but often excluded if using yarn/pnpm differently. 
# Usually best to keep it, but standard gitignore usually keeps it. I will keep it for stability, but ignore modules.)

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
backend/.env
frontend/.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Testing
coverage/

# Production build artifacts
build/
dist/

# System
.DS_Store
Thumbs.db

# Optional: Ignore uploaded files if they are local-only test uploads
# backend/uploads/*
# !backend/uploads/.gitkeep
